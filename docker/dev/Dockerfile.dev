FROM eclipse-temurin:21-jre-alpine

WORKDIR /app

# JAR 파일 복사 (build/libs에서)
COPY build/libs/*.jar app.jar

# 타임존 설정
ENV TZ=Asia/Seoul

# 포트 노출
EXPOSE 8083
EXPOSE 9001

# 애플리케이션 실행
ENTRYPOINT ["java", "-jar", "app.jar"]

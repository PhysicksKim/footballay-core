spring:
  datasource:
    url: jdbc:h2:mem:gyechunsik-scoreboard
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

  h2:
    console:
      enabled: true
      path: /h2-console

  devtools:
    restart:
      enabled: false
  security:
    user:
      name: admin
      password: admin
  data:
    redis:
      host: localhost
      port: 6379
      password: 1234

server:
  port: 8083
  ssl:
    key-store: classpath:keystore.p12
    key-store-password: -wva9.ea7)}}
    key-store-type: PKCS12
    key-alias: myalias
    enabled: true

# 개발 환경에서 허용할 CORS 출처
cors:
  allowedorigins: "http://localhost:8080,https://localhost:8080"

logging:
  level:
    org:
      testcontainers: debug
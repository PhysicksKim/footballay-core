spring:
  jpa:
    hibernate:
      ddl-auto: none

server:
  error:
    whitelabel:
      enabled: false

# 운영 환경에서 허용할 CORS 출처
cors:
  allowedorigins: "https://physickskim.github.io,https://gyechunsik.site,https://static.gyechunsik.site"